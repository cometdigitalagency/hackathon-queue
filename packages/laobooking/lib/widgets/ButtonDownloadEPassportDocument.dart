import 'package:flutter/material.dart';

class ButtonDownloadEPassportDipServicePDF extends StatelessWidget {
  const ButtonDownloadEPassportDipServicePDF({super.key});

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      child: ElevatedButton(
        onPressed: () {},
        child: Text("ດາວໂຫຼດ ຄຳຮ້ອງຂໍໜັງສືຜ່ານແດນ"),
      ),
    );
  }
}
